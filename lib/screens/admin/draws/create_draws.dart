import 'package:flutter/material.dart';

class CreateDraws extends StatelessWidget{
  const CreateDraws({super.key});
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Create Draws screen"),
    );
  }
}